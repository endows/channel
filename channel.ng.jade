.row
  .col-md-6.col-md-push-3.col-md-pull-3.panel.panel-default.text-center
    img.img-circle(ng-src="{{current_channel.user.image}}")
    h1 {{current_channel.user.name}}/{{current_channel.name}}
  div(ng-repeat='p in posts')
    .col-md-8.col-md-push-2.panel.panel-default
      .panel-body
        h1 {{p.body}}
  //- p create Post
  //-   input(ng-model='new_post')
  //-   button(ng-click='createPost(new_post)')
